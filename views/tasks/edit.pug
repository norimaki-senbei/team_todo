extends ../layout

block content 
  h2 タスクの編集
    //a(href="/examples") Example
    //| / edit
  form(action=`/teams/${ teamId }/tasks/${ task.id }`, method="post")
    _csrf
    _method put


    div
      label(for="taskTitle") タイトル
      input(name="taskTitle", value=task.title)
    div
      label(for="taskBody") 内容
      textarea(name="taskBody", rows="3", value=task.body)

    div
      button(type="submit") 送信