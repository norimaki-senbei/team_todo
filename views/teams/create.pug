extends ../layout

block content 
  h2 チーム作成
  form.box(action=`/teams`, method="post")
    _method post
    _csrf

    - if(err)
      ul.errors
        each error in err.errors
          li= error.message

    div
      p チーム名を入力してね
      input(name="teamName")

    div
      input(type="submit")